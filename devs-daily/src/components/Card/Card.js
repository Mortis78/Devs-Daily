import React from 'react'
import './Card.css';

const Card = (props) => {
  return (

    <section>
        {/* <Link className='' to={`/${props.id}`}>
            <span className=''>
                <img className="" src={props.} alt={'img'} />
                <h3 className=''>{props.}</h3>
            </span>
        </Link>
        <span className=''>
            <p>{props.}</p>
        </span> */}
    </section>
    )
}